// catch every visible window titles and store as hwnd: hash(title)
// add when window watcher catch new window attached
// scan every 30 sec for title changes
// if violation is found check again after 5sec then report/terminate